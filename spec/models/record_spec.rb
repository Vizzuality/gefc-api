require "rails_helper"

RSpec.describe Record, type: :model do
  # let(:indicator) { create(:indicator) }
  # let!(:region1) { create(:region) }
  # let!(:geometry1) { create(:geometry_polygon, region: region1) }
  # let!(:region2) { create(:region) }
  # let!(:geometry2) { create(:geometry_polygon, region: region2) }
  # let!(:record) { create(:record, indicator: indicator, year: 2020, region: region1) }
  # let!(:record2) { create(:record, indicator: indicator, year: 2021, region: region2) }
  # let!(:record3) { create(:record, indicator: indicator, year: 2021, region: region2) }

  # describe :update_indicator_region_ids do
  #   it "after create push all region ids without duplication" do
  #     expect(indicator.region_ids.count).to eq(2)
  #     expect(indicator.region_ids.include?(region1.id)).to eq(true)
  #     expect(indicator.region_ids.include?(region2.id)).to eq(true)
  #   end
  # end
end
